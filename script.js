let choices = ["rock", "paper", "scissors"];

function getComputerChoice() {
    let random_number = Math.floor(Math.random()*3);
    let decision = choices[random_number];
    return decision;
}



/*

an array called choices that contains rock, paper, and scissors

function getComputerChoice
    a variable that makes a random number
    a variable called decision that uses the random number to get a value from the choices array
    return the computer_choice value


function runGame
    a variable called computer




*/